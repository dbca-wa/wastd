machine:
  python:
    version: 2.7

dependencies:
  pre:
    - easy_install functools
    - mkdir -p /home/ubuntu/wastd/logs/
    - touch /home/ubuntu/wastd/logs/wastd.log
    - pip install -r requirements/dev.txt

database:
  post:
    - psql -c "CREATE EXTENSION postgis;" -U ubuntu -d circle_test

test:
   override:
    - echo "https://regebro.wordpress.com/2010/12/13/python-implementing-rich-comparison-the-correct-way/"
    - python manage.py check
#     - coverage run --source="." manage.py test --settings=config.settings.test
#     - coverage html
#   post:
#     - coveralls
